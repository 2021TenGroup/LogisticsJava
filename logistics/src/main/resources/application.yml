server:
  servlet:
    context-path: /Logistics
  port: 8089
spring:
  datasource:
    url: jdbc:mysql://8.140.112.152:3306/lms?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Root!!2018
mybatis:
  #  全局配置文件
  #  config-location: classpath:mybatis/mybatis-config.xml
  #  sql映射文件
  mapper-locations: classpath:/mapper/**/*.xml
  #  写了这个配置就不用写全局配置文件了
  configuration:
    map-underscore-to-camel-case: true
